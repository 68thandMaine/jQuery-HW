$(document).ready(function() {

var text = function(speak) {
  return speak ;
}
var speak = prompt("Write a four letter word please");
var result = text(speak);
// // alert(result);
//
// var first = function(result){
//   return result.toUpperCase().charAt(0);
// }

// var total = function(result) {
//   return result.length;
// }
// var last = function(result){
//   return result.toUpperCase().charAt(total);
// }

// newResult.length;

speak.split(" ");
console.log(splitSTring)


var newResult = first(result)
alert(newResult);
});
